import { createClient } from '@supabase/supabase-js'

// Create a single supabase client for interacting with your database

// export const supabase = createClient(process.env.VITE_DB_URL, import.meta.env.VITE_DB_KEY)
